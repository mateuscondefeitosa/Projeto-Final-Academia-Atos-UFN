@model List<DreamsMade.Models.Post>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


<div class="container">
        <h1 class="d-flex justify-content-center my-5">My Dreams</h1>
        <div><a class="nav-link" id="navlink" asp-area="" asp-controller="Home" asp-action="NewPost">Click Here to make a new Post!</a></div>
        @foreach (var post in @Model)
        {
            <div class="card mb-3">
                <div class="row">
                    <div class="col-md-4">
                        <img src="@post.image" class="img-fluid">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body" style="border: none;">
                            <a href="@Url.Action("Post","Home", new { id = @post.id })"><h5 class="card title">
                                @post.title
                            </h5></a>
                            <p class="card-text">
                                @post.text
                            </p>
                            <p class="card-text">
                                <small class="text-muted">
                                    @post.user.name
                                </small>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        }
</div>
